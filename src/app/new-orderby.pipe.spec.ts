import { NewOrderbyPipe } from './new-orderby.pipe';

describe('NewOrderbyPipe', () => {
  it('create an instance', () => {
    const pipe = new NewOrderbyPipe();
    expect(pipe).toBeTruthy();
  });
});
